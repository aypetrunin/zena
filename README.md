# Zena — Platform Architecture (Root Repository)

Этот репозиторий — **корневая архитектура (root repo)** системы **Zena**.

Он **не содержит бизнес-код сервисов**.
Его задача — зафиксировать **архитектурный снимок всей системы**:
какие сервисы используются и **в каких версиях (commit SHA)**.

---

## 📦 Что содержит этот репозиторий

* файлы запуска и оркестрации:

  * `docker-compose.yml`
  * `Makefile`
* документацию по работе с платформой
* **Git submodules** с сервисами:

  * `apifast` — [https://github.com/aypetrunin/apifast](https://github.com/aypetrunin/apifast)
  * `langgraph` — [https://github.com/aypetrunin/langgraph](https://github.com/aypetrunin/langgraph)
  * `mcpserver` — [https://github.com/aypetrunin/mcpserver](https://github.com/aypetrunin/mcpserver)

> ⚠️ Важно
> Каждый сервис — **отдельный Git-репозиторий**.
> Root-репо **не копирует их код**, а лишь указывает:
>
> → *какой именно коммит каждого сервиса используется вместе*

---

## 🧠 Ключевая идея

**Root-репозиторий = снимок всей системы в конкретный момент времени.**

Он отвечает на вопрос:

> *«Какие версии всех сервисов считаются совместимыми прямо сейчас?»*

---

## 📁 Структура репозитория

```text
zena/
├── .gitignore
├── Makefile
├── docker-compose.yml
├── README.md
├── README docker.md
├── README make.md
├── README uv.md
├── apifast/     (git submodule)
├── langgraph/   (git submodule)
└── mcpserver/   (git submodule)
```

---

## 🧩 Что такое Git submodule (коротко)

Submodule — это:

* ссылка на внешний Git-репозиторий
* зафиксированная версия (commit SHA)
* **НЕ копия кода**

Визуально:

```text
zena
 ├── apifast     → github.com/aypetrunin/apifast @ <commit>
 ├── langgraph   → github.com/aypetrunin/langgraph @ <commit>
 └── mcpserver   → github.com/aypetrunin/mcpserver @ <commit>
```

Root-репо хранит **только указатели на версии**.

---

## ❌ Что ЗАПРЕЩЕНО делать (очень важно)

🚫 **Никогда не делай следующее:**

* не писать код сервисов в root-репозитории
* не коммитить файлы сервисов в `zena`
* не хранить `.env`, API-ключи, JSON-секреты в Git
* не делать `git add .` без понимания, что именно добавляется
* не править код сервисов, находясь в root-репо

> Если ты редактируешь код сервиса — ты должен быть **в его репозитории**, а не в root.

---

## ✅ Правильный рабочий процесс

### 🧑‍💻 1. Работа с конкретным сервисом (пример: `apifast`)

```bash
cd apifast
git status
```

Создаём фичу:

```bash
git checkout -b feature/my-feature
```

Работаем как с обычным репозиторием:

```bash
git add -A
git commit -m "Add my feature"
git push -u origin feature/my-feature
```

📌 **Важно:**
На этом этапе root-репозиторий **вообще не участвует**.

---

### 🔀 2. Мёрдж фичи в сервисе

После того как PR замёрджен в `main` сервиса:

```bash
cd apifast
git checkout main
git pull
```

---

### 🧩 3. Фиксация новой версии сервиса в архитектуре

Возвращаемся в root:

```bash
cd ..
git status
```

Ты увидишь:

```text
modified: apifast (new commits)
```

Это означает:

> submodule указывает на новый commit

Фиксируем архитектуру:

```bash
git add apifast
git commit -m "Bump apifast submodule"
git push
```

✅ Теперь **вся команда** получит обновлённую версию сервиса.

---

## 🔄 Обновление всех сервисов разом

Обновить все submodules до последних коммитов их `main`-веток:

```bash
git submodule update --remote --merge
git add .
git commit -m "Bump submodules"
git push
```

Использовать **осознанно**, обычно после согласованного обновления.

---

## 🔁 Повседневные команды (памятка)

### После `git pull` в root-репо

```bash
git pull
git submodule update --init --recursive
```

### Проверить версии сервисов

```bash
git submodule status
```

### Зайти в сервис

```bash
cd apifast
```

### Вернуться в root

```bash
cd ..
```

---

## 🧩 Ментальная модель (запомнить)

* **Сервисы** — живут своей жизнью, со своими репозиториями
* **Root-репо** — фиксирует, какие версии сервисов считаются совместимыми
* **Код → в сервисах**
* **Архитектура → в root**

---

Если хочешь, дальше можем:

* добавить раздел *CI/CD и релизная модель*
* описать *локальную разработку vs production*
* сделать короткий `TL;DR` для новых разработчиков
* подготовить `CONTRIBUTING.md` под эту модель

Скажи, что нужно следующим шагом.
